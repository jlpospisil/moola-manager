import { StyleSheet } from 'react-native';

const styles = {
    container: {
        flex: 1,
        width: "100%",
        justifyContent: "center",
        alignItems: "center"
    },
    fullWidth: {
        alignSelf: "stretch"
    }
};

export default StyleSheet.create(styles);